[PARSER]
    Name        audit_json
    Format      regex
    # Matches Laravel log format: [YYYY-MM-DD HH:MM:SS] namespace.LEVEL: data: {json}
    Regex       ^\[(?<ts>[^\]]+)\] (?<ns>\w+)\.(?<level>\w+):\s+data:\s+(?<log>.*)$

[PARSER]
    Name        json
    Format      json

